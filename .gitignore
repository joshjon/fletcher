# Build / release artifacts
/bin/
/dist/
build/
tmp/

# Bundled Firecracker VMM assets - fetched by `make fetch-vmm`, embedded at
# build time. Kept out of git (multi-megabyte blobs); about.txt is committed so
# the embed directive still compiles on a fresh checkout.
internal/runtime/firecrackerdriver/vmm/assets/*/firecracker
internal/runtime/firecrackerdriver/vmm/assets/*/vmlinux

# Built guest-agent binary (the microVM init) - built from cmd/fletcher-guest
# by `make build` and embedded; about.txt is committed.
internal/runtime/firecrackerdriver/guestagent/assets/*/fletcher-guest
# Stray guest binary if cmd/fletcher-guest is built directly into the repo root.
/fletcher-guest

# Test / coverage
coverage.out
coverage.html

# Local SQLite files
*.db
*.db-journal
*.db-shm
*.db-wal

# Local config / secrets
fletcher.toml
*.age

# Lefthook local overrides
lefthook-local.yml
.lefthook-local.yml

# Logs
*.log

# Misc local
local/

# macOS
.DS_Store

# IDE
.idea/
.vscode/
*.swp
